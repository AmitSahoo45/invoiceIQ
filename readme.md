# InvoiceIQ

InvoiceIQ is a lightweight Next.js frontend that lets you upload invoice PDFs or images and immediately preview the files you selected. It is intended as a starting point for building an invoice ingestion or analysis workflow.

## Project structure

- `frontend/` — Next.js app with the invoice upload UI.

## Getting started

```bash
cd frontend
npm install
npm run dev
```

Then open [http://localhost:3000](http://localhost:3000) to use the invoice uploader.
